.. _basic_config_file:

Basic XML configuration
***********************

This test case creates a server and two clients of that server.
Each of the clients has a publisher and a subscriber on two different topics.
In addition, a snapshot of the discovery database is taken at the second five of the execution, which is the state
the test suite compares against the expected one.

.. literalinclude:: ../02-resources/tests/test_01_trivial.xml
    :language: xml
    :linenos:

The snapshot generated by this scenario is the following one:

.. literalinclude:: ../02-resources/tests/test_01_trivial.snapshot
    :language: xml
    :linenos:
